# Настройка GitHub Pages (ручная генерация)

## Обзор

Инструкция по настройке GitHub Pages с ручной генерацией HTML из Markdown файлов.

**В проекте два независимых сайта:**
1. **Лендинг курса** (`website_output/`) — главная страница курса
2. **База знаний** (`30 aimindset/website_output/`) — база знаний с материалами

Каждый сайт публикуется в **отдельный репозиторий** на GitHub.

---

## 🎯 ЛЕНДИНГ КУРСА

### Шаг 1: Создайте репозиторий для лендинга

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий: `ai-course`
3. **НЕ** добавляйте README, .gitignore или лицензию

### Шаг 2: Инициализируйте Git для лендинга

```bash
cd "40 project/AI productivity course {ai-prod-crs}"
git init
git add .gitignore README.md
git add website_output/  # Исходные HTML файлы (опционально)
git add generate_landing_for_pages.py
git commit -m "Initial commit: лендинг курса"
git branch -M main
git remote add origin https://github.com/vkamensky/ai-course.git
git push -u origin main
```

### Шаг 3: Настройте GitHub Pages для лендинга

1. Перейдите в репозиторий на GitHub
2. **Settings** → **Pages**
3. **Source:** выберите "Deploy from a branch"
4. **Branch:** `main` / `docs`
5. **Folder:** `/docs`
6. Нажмите **Save**

### Шаг 4: Сгенерируйте лендинг

```bash
python3 generate_landing_for_pages.py
```

Это создаст папку `docs/` с `index.html` (последняя версия лендинга).

### Шаг 5: Закоммитьте и запушьте

```bash
git add docs/
git commit -m "Первая генерация лендинга для GitHub Pages"
git push
```

### Шаг 6: Проверьте лендинг

Через 1-2 минуты лендинг будет доступен:
`https://vkamensky.github.io/ai-course/`

---

## 📚 БАЗА ЗНАНИЙ

### Шаг 1: Создайте репозиторий для базы знаний

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий: `ai-course-kb`
3. **НЕ** добавляйте README, .gitignore или лицензию

### Шаг 2: Инициализируйте Git для базы знаний

```bash
cd "40 project/AI productivity course {ai-prod-crs}"
git init
git add .gitignore README.md
git add "30 aimindset/"  # Markdown файлы и скрипты
git commit -m "Initial commit: база знаний курса"
git branch -M main
git remote add origin https://github.com/vkamensky/ai-course-kb.git
git push -u origin main
```

### Шаг 3: Настройте GitHub Pages для базы знаний

1. Перейдите в репозиторий на GitHub
2. **Settings** → **Pages**
3. **Source:** выберите "Deploy from a branch"
4. **Branch:** `main` / `docs`
5. **Folder:** `/docs`
6. Нажмите **Save**

### Шаг 4: Сгенерируйте базу знаний

```bash
cd "30 aimindset"
python3 generate_knowledge_base_for_pages.py
```

Это создаст папку `docs/` в корне проекта с базой знаний.

### Шаг 5: Закоммитьте и запушьте

```bash
cd ../..
git add docs/
git commit -m "Первая генерация базы знаний для GitHub Pages"
git push
```

### Шаг 6: Проверьте базу знаний

Через 1-2 минуты база знаний будет доступна:
`https://vkamensky.github.io/ai-course-kb/`

---

## Ежедневный workflow

### Обновление лендинга:

1. Редактируете HTML в `website_output/` (через Cursor)
2. Генерируете:
   ```bash
   python3 generate_landing_for_pages.py
   ```
3. Коммитите и пушите:
   ```bash
   git add docs/
   git commit -m "Обновлен лендинг"
   git push
   ```

### Обновление базы знаний:

1. Редактируете Markdown файлы в `30 aimindset/` (через Obsidian)
2. Генерируете:
   ```bash
   cd "30 aimindset"
   python3 generate_knowledge_base_for_pages.py
   ```
3. Коммитите и пушите:
   ```bash
   cd ../..
   git add docs/
   git add "30 aimindset/*.md"  # Если изменили контент
   git commit -m "Обновлена база знаний"
   git push
   ```

---

## Структура репозиториев

### Репозиторий лендинга (`ai-course`):

```
ai-course/
├── .gitignore
├── README.md
├── generate_landing_for_pages.py
├── website_output/          # Исходные HTML (опционально)
│   └── ai-productivity-v*.html
└── docs/                    # ← HTML для GitHub Pages (коммитим)
    └── index.html
```

### Репозиторий базы знаний (`ai-course-kb`):

```
ai-course-kb/
├── .gitignore
├── README.md
├── 30 aimindset/
│   ├── generate_knowledge_base_for_pages.py
│   ├── generate_site.py
│   ├── website_output/      # Временная папка (НЕ коммитим)
│   └── *.md                # Исходный контент
└── docs/                    # ← HTML для GitHub Pages (коммитим)
    ├── index.html
    ├── css/
    ├── images/
    └── ...
```

---

## Важно

- ✅ Каждый сайт в **отдельном репозитории**
- ✅ Используйте **отдельные скрипты** для генерации каждого сайта
- ✅ **Коммитьте** папку `docs/` в каждом репозитории
- ❌ **НЕ коммитьте** временные папки `website_output/`

---

## Troubleshooting

### Сайты не обновляются
- Подождите 1-2 минуты (кеш GitHub Pages)
- Проверьте, что вы закоммитили папку `docs/` в правильном репозитории
- Убедитесь, что в Settings → Pages выбрана папка `/docs`

### Ошибки при генерации
- Убедитесь, что используете правильный скрипт для каждого сайта
- Проверьте, что все пути в скрипте правильные
- Посмотрите на ошибки в консоли

### Файлы не появляются в docs/
- Проверьте, что скрипт выполнился без ошибок
- Убедитесь, что папка `docs/` создалась
- Попробуйте запустить скрипт еще раз

---

## Полезные команды

```bash
# Посмотреть статус репозитория
git status

# Посмотреть изменения в docs/
git diff docs/

# Удалить docs/ и перегенерировать
rm -rf docs/
python3 generate_landing_for_pages.py  # или generate_knowledge_base_for_pages.py
```
